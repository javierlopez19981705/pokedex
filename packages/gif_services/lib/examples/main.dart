// import 'package:gif_services/src/gif_services.dart';

void main() async {
  // final service = GifServices();
  // final list = await service.fetchGifs();
  // for (final element in list) {
  //   // print(element);
  // }
}
